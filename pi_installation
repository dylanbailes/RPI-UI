sudo apt install -y \
    python3-pip \
    python3-venv \
    python3-pyqt5 \
    libusb-1.0-0 \
    libavcodec-dev \
    libavformat-dev \
    libswscale-dev \
    libswresample-dev \
    libavutil-dev \
    build-essential


    https://www.flir.com/products/spinnaker-sdk/



    cd ~/Downloads
tar -xf spinnaker-*arm64*.tar.gz
cd spinnaker-*arm64*/
cat README    # check supported distros first
sudo sh install_spinnaker_arm.sh
# Say YES to the usbfs memory limit question



# Check Python version — should be 3.11 on Bookworm
python3 --version

# Create venv
python3 -m venv ~/mccb_env
source ~/mccb_env/bin/activate



pip install ~/Downloads/spinnaker_python-*cp311*.whl


python3 -c "import PySpin; print('PySpin OK')"




pip install pyserial numpy opencv-python-headless

# PyQt5 is better installed from apt on Pi to avoid compile issues
# but link it into the venv
sudo apt install -y python3-pyqt5



# Recreate venv with system packages flag
deactivate
rm -rf ~/mccb_env
python3 -m venv ~/mccb_env --system-site-packages
source ~/mccb_env/bin/activate

# Now reinstall pip packages
pip install pyserial numpy opencv-python-headless
pip install ~/Downloads/spinnaker_python-*cp311*.whl




echo "source ~/mccb_env/bin/activate" >> ~/.bashrc



source ~/mccb_env/bin/activate

python3 -c "import PySpin; print('PySpin OK')"
python3 -c "import serial; print('PySerial OK')"
python3 -c "import cv2; print('OpenCV OK')"
python3 -c "from PyQt5.QtWidgets import QApplication; print('PyQt5 OK')"